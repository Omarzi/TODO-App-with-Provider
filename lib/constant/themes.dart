import 'package:flutter/material.dart';

class AppTheme {
  // Static Colors
  static Color? backgroundcolor = Colors.teal[400];
  static Color primarycolor = const Color(0xff);
  static Color text = const Color(0xff);
  static Color blackColor = const Color(0xff242424);
  static Color whiteColor = Colors.white;
  static Color? floatingactionButtonColor = Colors.indigo[400];
  static Color? darkBlue = Colors.blue[800];
  static Color? tealColor = Colors.teal[400];
}
